# Deprecated

Store rejected or retired method cards here when they are worth preserving for historical context.

Do not use deprecated cards at runtime unless the user explicitly asks to inspect past decisions.
