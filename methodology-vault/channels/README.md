# Channels

Channel files hold workspace or personal preferences that modify how shared method cards are selected.

They should not duplicate the method body. Keep only routing preferences, preferred defaults, local constraints, and examples.
