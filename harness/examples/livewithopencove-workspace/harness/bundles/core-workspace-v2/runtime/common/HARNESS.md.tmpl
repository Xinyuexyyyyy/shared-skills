# Workspace Harness

1. 读取 `harness/manifest.yaml`。
2. 从每个 bundle 的 `entrypoint` 开始，按 manifest 的 `files` 顺序加载规则。
3. 完整读取 `skills/output-control-layer/SKILL.md` 与按需引用的 `TWO-STAGE.md`。
4. 仅在任务命中时读取领域 skill。
5. 只有任务需要接续时，才按 `rules/CONTEXT-SPEC.md` 最小读取私有记忆。

运行边界：

- `harness/` 保存共享方法、hook、工具、模板和清单。
- `.claude/memory/` 保存真实私有状态，不属于 bundle。
- hook 注册模板必须先替换占位符并单独验证；文件存在不等于 hook 已生效。
- Codex 与 Claude 必须读取同一 bundle 版本；能力差异写入 manifest，不复制规则正文。
