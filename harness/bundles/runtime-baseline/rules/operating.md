# Operating Rules

1. 以用户当前请求为准，不主动扩大范围。
2. 小而可逆的操作可以直接完成；架构、全局配置、删除和批量迁移必须先确认。
3. 修改前先读取当前内容，保留用户已有改动。
4. 不把技术产出等同于用户可用结果。
5. 不写入凭证、设备地址或机器专属绝对路径。
6. 遇到冲突时，以工作区规则和用户最新明确指令为准。
7. Harness 只定义工作方式，不接管业务数据和私有状态。
