# Idea-to-Research Skill 环境变量配置模板
# 复制此文件为 .env 并根据实际情况修改

# 工作区根目录（可选）
# 默认：通过相对路径向上 3 层自动推断
# 如果 skill 安装位置不在 <workspace>/shared-skills/idea-to-research/，需要手动设置
# WORKSPACE_ROOT=/path/to/your/workspace

# harvest-tool 依赖路径（可选）
# 默认：<workspace>/skills/harvest-tool/
# 仅在 github-build 路线下需要，其他路线不受影响
# HARVEST_TOOL_PATH=/path/to/harvest-tool

# 使用示例：
# 1. 复制此文件：cp .env.example .env
# 2. 根据实际情况取消注释并修改路径
# 3. 加载环境变量：source .env 或在命令前添加 export
