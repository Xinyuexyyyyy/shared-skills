# 开发日志 — AI-Info-Digest Round 1

**时间**：2026-04-13  
**节点**：⑤ 开发

---

## 完成内容

### 文件结构
```
blueprints/ai-info-digest/
├── config.json          # 配置文件（HN + RSS 源，AI 配置）
├── main.py              # 主入口，6 步流水线
├── src/
│   ├── __init__.py
│   ├── models.py        # ContentItem 数据模型
│   ├── scrapers/
│   │   ├── __init__.py
│   │   ├── hackernews.py  # HN Firebase API 抓取
│   │   └── rss.py         # feedparser RSS 抓取
│   ├── scorer.py         # AI 批量评分（qwen3.5-flash）
│   ├── dedup.py          # URL 归一化去重 + 已推送记录
│   └── pusher.py         # 飞书推送（预留 feishu_im_user_message）
```

### 流水线
```
Fetch HN → Fetch RSS → URL 去重 → 过滤已推送 → AI 批量评分 → 阈值过滤 → 飞书推送
```

### 修复的问题
- 相对导入路径：`from .models` → `from ..models`（`src/` 包内正确导入父模块）

---

## 待验证

- [ ] 实际运行 `python3 main.py`（需先清理环境变量中的 proxy，或在 ecs 上运行）
- [ ] 飞书推送连通性测试（需配置 target_open_id）
- [ ] systemd timer 部署

---

## 依赖

- Python 3.11+
- httpx
- feedparser

安装：`pip install httpx feedparser`
